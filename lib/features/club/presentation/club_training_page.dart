
import 'package:flutter/material.dart';

class ClubTrainingPage extends StatelessWidget {
  const ClubTrainingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Club Training'),
      ),
      body: const Center(
        child: Text('Club Training Page'),
      ),
    );
  }
}
