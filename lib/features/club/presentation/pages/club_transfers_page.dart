
import 'package:flutter/material.dart';

class ClubTransfersPage extends StatelessWidget {
  const ClubTransfersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Club Transfers'),
      ),
      body: const Center(
        child: Text('Club Transfers Page'),
      ),
    );
  }
}
