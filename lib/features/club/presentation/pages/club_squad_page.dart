
import 'package:flutter/material.dart';

class ClubSquadPage extends StatelessWidget {
  const ClubSquadPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Club Squad'),
      ),
      body: const Center(
        child: Text('Club Squad Page'),
      ),
    );
  }
}
