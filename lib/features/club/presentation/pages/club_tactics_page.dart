
import 'package:flutter/material.dart';

class ClubTacticsPage extends StatelessWidget {
  const ClubTacticsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Club Tactics'),
      ),
      body: const Center(
        child: Text('Club Tactics Page'),
      ),
    );
  }
}
