
import 'package:flutter/material.dart';

class ClubStaffPage extends StatelessWidget {
  const ClubStaffPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Club Staff'),
      ),
      body: const Center(
        child: Text('Club Staff Page'),
      ),
    );
  }
}
