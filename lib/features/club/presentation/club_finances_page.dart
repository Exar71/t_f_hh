
import 'package:flutter/material.dart';

class ClubFinancesPage extends StatelessWidget {
  const ClubFinancesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Club Finances'),
      ),
      body: const Center(
        child: Text('Club Finances Page'),
      ),
    );
  }
}
